/* STREAMING_CHUNK:Styling the transparent overlay /
#idle-screen {
position: fixed;
top: 0;
left: 0;
width: 100vw;
height: 100vh;
background-color: transparent; / No background, lets the Matrix canvas shine through! */
z-index: 9999;
display: flex;
justify-content: center;
align-items: center;
opacity: 0;
pointer-events: none;
transition: opacity 1.5s ease-in-out;
}

/* STREAMING_CHUNK:Defining active state and hiding HUD elements */
#idle-screen.idle-active {
opacity: 1;
pointer-events: all;
}

/* When idle, fade out the HUD windows but keep them in their exact positions */
body.system-idle .window,
body.system-idle .shortcut-hint {
opacity: 0 !important;
pointer-events: none !important;
}

/* Ensure smooth fading in both directions without breaking your other animations */
.window {
transition: opacity 1.5s ease-in-out, box-shadow 0.5s ease, border-color 0.5s ease, background 0.5s ease !important;
}
.shortcut-hint {
transition: opacity 1.5s ease-in-out !important;
}

/* STREAMING_CHUNK:Defining the standby pulse animation /
.idle-reactor {
transform: scale(3); / Make the arc reactor massive in the center */
animation: idle-pulse 3s infinite alternate ease-in-out;
}

@keyframes idle-pulse {
0% {
box-shadow: 0 0 15px var(--hud-cyan), inset 0 0 15px var(--hud-cyan);
}
100% {
box-shadow: 0 0 60px var(--hud-cyan), inset 0 0 60px var(--hud-cyan);
}
}
